////////////////////////////////////////////////////////////////////////////////
//
// This file was generated using Swagger Codegen, specialised for Wotif.com
//
////////////////////////////////////////////////////////////////////////////////

package {{package}};

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import {{invokerPackage}}.ApiException;
import feign.*;
import feign.RequestLine;
import feign.codec.Decoder;
import feign.codec.EncodeException;
import feign.codec.Encoder;
import feign.jackson.JacksonDecoder;
import javax.inject.Named;
{{#imports}}import {{import}};
{{/imports}}
import java.io.IOException;
import java.util.*;

{{#operations}}
/**
 * Feign Proxy
 */
interface {{classname}}Proxy {
  {{#operation}}
  @RequestLine("{{httpMethod}} {{path}}")
  {{^get}}
  {{#consumes}}
  @Headers("Content-Type: {{mediaType}};charset=UTF-8")
  {{/consumes}}
  {{/get}}
  Response {{nickname}}({{#allParams}}@Named("{{paramName}}") {{{dataType}}} {{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}});
  
  {{/operation}}
}

/**
 * Green-Edge {{classname}} Client
 * generated using Swagger-Codegen
 */
public class {{classname}} {
  private String basePath;
  private {{classname}}Proxy feignProxy;
  private Encoder encoder;
  private Decoder decoder;

  //private constructor to ensure {{classname}} can't be instantiated without basePath
  @SuppressWarnings("unused")
  private {{classname}}() {}

  /**
   * @param basePath - the base URL path for the service
   */
  public {{classname}}(String basePath) {
      if(basePath == null) {
          throw new IllegalArgumentException("Base Path is null! Base Path for Green-Edge Service must be set before using client.");
      }
      setBasePath(basePath);
  }

  /**
   * Sets the base URL path for the service
   * @param basePath
   */
 public {{className}} setBasePath(String basePath) {
    this.basePath = basePath;
    this.decoder = new JacksonDecoder();
    this.encoder = new GreenEdgeEncoder();
    feignProxy = Feign.builder()
                     .encoder(encoder)
                     .decoder(decoder)
                     .target({{classname}}Proxy.class, basePath);
    return this;
  }
  
  /**
   * Gets the base URL path for the service
   * @return String
   */
  public String getBasePath() {
    return basePath;
  }

  {{#operation}}
  public {{#returnType}}{{{returnType}}} {{/returnType}}{{^returnType}}Response {{/returnType}} {{nickname}} ({{#allParams}}{{{dataType}}} {{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}})
  {{#returnType}} 
  throws IOException
  {{/returnType}}
  {{#requiredParamCount}}
  throws ApiException
  {{/requiredParamCount}}
  {
    {{#requiredParamCount}}
    // verify required params are set
    if({{/requiredParamCount}}{{#requiredParams}} {{paramName}} == null {{#hasMore}}|| {{/hasMore}}{{/requiredParams}}{{#requiredParamCount}}) {
       throw new ApiException(400, "missing required params");
    }
    {{/requiredParamCount}}
    {{^returnType}}
      return feignProxy.{{nickname}}({{#allParams}}{{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}});
    {{/returnType}}
    {{#returnType}}
      Response resp = feignProxy.{{nickname}}({{#allParams}}{{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}});
      return {{returnType}}.class.cast(decoder.decode(resp, {{returnType}}.class));
    {{/returnType}}
  }

  {{/operation}}
}
{{/operations}}

/**
 * GreenEdgeEncoder will encode the just the body of any posted request parameter named "body",
 * in other cases it is equivalent to the feign JacksonEncoder.
 */
class GreenEdgeEncoder implements Encoder {

    private final ObjectMapper mapper;

    public GreenEdgeEncoder() {
        mapper = new ObjectMapper()
                 .setSerializationInclusion(JsonInclude.Include.NON_NULL)
                 .configure(SerializationFeature.INDENT_OUTPUT, true);
    }

    @Override
    public void encode(Object object, RequestTemplate template) throws EncodeException {
        try {
            Object body = ((LinkedHashMap)object).get("body");
            if(body == null) {
                template.body(mapper.writeValueAsString(object));
            } else {
                template.body(mapper.writeValueAsString(body));
            }
        } catch (JsonProcessingException e) {
            throw new EncodeException(e.getMessage(), e);
        }
    }
}
